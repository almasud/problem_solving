import daily_code_challenge.DailyCodeChallenge

/**
 * The main method where we call the solution function to evaluate the result
 */
fun main() {
    println(DailyCodeChallenge.frequencySort("Aabb"))
}
